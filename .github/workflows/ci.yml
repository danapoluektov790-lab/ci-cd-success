name: Simple CI/CD Test

on: [push, workflow_dispatch]

jobs:
  simple-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        
    - name: 🧪 Simple test
      run: |
        echo "🎉 GitHub Actions работает!"
        echo "Создаем базовую структуру..."
        mkdir -p src tests
        
        # Простой калькулятор
        echo "class Calculator:" > src/calculator.py
        echo "    def add(self, a, b):" >> src/calculator.py
        echo "        return a + b" >> src/calculator.py
        
        # Простой тест
        echo "def test_add():" > tests/test_calc.py
        echo "    assert 2 + 3 == 5" >> tests/test_calc.py
        
        echo "✅ Файлы созданы!"
        
    - name: 📊 Verify
      run: |
        echo "Проверяем созданные файлы:"
        ls -la src/
        ls -la tests/
        python -c "from src.calculator import Calculator; c = Calculator(); print(f'2+3={c.add(2,3)}')"
        echo "✅ Всё работает!"
        
    - name: 🎉 Success
      run: |
        echo " "
        echo "🎉🎉🎉 CI/CD УСПЕШНО ЗАПУЩЕН! 🎉🎉🎉"
        echo "GitHub Actions работает корректно!"
        echo " "
